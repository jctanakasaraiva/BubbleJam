using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class NicheController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }
    public void OnButtonClicked(string buttonName)
    {
        /*
         using UnityEngine;
            using UnityEngine.UI;

            public class DisplayLastButtonClicked : MonoBehaviour
            {
                public Text displayText; // Arraste o Text que exibirá o resultado

                void Start()
                {
                    string lastButtonClicked = PlayerPrefs.GetString("LastButtonClicked", "Nenhum botão foi clicado");
                    displayText.text = "Você clicou no botão: " + lastButtonClicked;
                }
            }

        */

    }

}
